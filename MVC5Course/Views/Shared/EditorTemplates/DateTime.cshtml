@model DateTime?
@if (Model.HasValue)
{
    @Html.TextBox("", Model.Value.ToString("yyyy/MM/dd"), new { @class = "form-control text-box single-line", style = "width:120px" })
}
else
{
    @Html.TextBox("", "", new { @class = "form-control text-box single-line", style = "width:120px" })
}
