@{
    int[] items = ViewBag.items;
  }
@for (int i = 0; i < items.Length; i++)
 {
     @items[i].ToString();
 }

@for (int i = 0; i < items.Length; i++)
{
var item = items[i].ToString() + " ";
@item
}

<ul>
@for (int i = 0; i < items.Length; i++)
{
var item = items[i].ToString();

  <li>@item</li>
}
</ul>
